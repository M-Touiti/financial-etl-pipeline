# Core
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
pydantic-settings==2.3.0

# Data processing
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0          # fast parquet support for pandas

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.0
psycopg2-binary==2.9.9   # for sync operations (Alembic)

# Kafka
aiokafka==0.11.0

# Scheduling
apscheduler==3.10.4

# Logging
structlog==24.2.0

# HTTP client (for external data sources)
httpx==0.27.0

# Dev / Test
ruff==0.4.4
pytest==8.2.0
pytest-asyncio==0.23.0
pytest-cov==5.0.0
testcontainers[kafka,postgresql]==4.7.0
faker==25.0.0
aiosqlite==0.20.0        # SQLite async driver for tests
